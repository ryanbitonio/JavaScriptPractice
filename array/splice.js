const letters = ["a", "b", "c", "d"];

let spliced = letters.splice(1, 3);
console.log(spliced);

// slice(start, end)
// start = included , end = up to array.length
// changes the original value of the array
