const letters = [1, 2, 3, 4, 5];

let sliced = letters.slice(1, 4);
console.log(sliced);

// slice(start, end)
// start = included , end = up to array.length - 1
// does not change the original value of the array
